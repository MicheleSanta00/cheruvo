# Requirements per GitHub Actions
# Leggero: VADER + AV scores (niente PyTorch/FinBERT)
# NON usare per Render — usa backend/requirements.txt

# Core DB
psycopg2-binary==2.9.9
pandas==2.2.2
requests==2.32.3
python-dotenv==1.0.1
python-dateutil==2.9.0

# News sources
feedparser==6.0.11
newsapi-python==0.2.7

# Sentiment (leggero, niente GPU)
vaderSentiment==3.3.2

# Sentiment LLM (Groq — qualità finanziaria per news non-AV)
groq==0.9.0

# Alert email
resend==2.0.0

# Necessari per importare i moduli digest.py ed earnings.py dal cron
# (definiscono router FastAPI a livello di modulo; auth.py usa httpx).
# Il cron ne usa solo le funzioni di logica, ma l'import tira dentro questi.
fastapi==0.111.0
httpx==0.27.2

# Date earnings (usate da refresh_earnings nel cron)
yfinance==0.2.40
